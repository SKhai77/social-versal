// Importing the User and Thought models
const User = require("./User");
const Thought = require("./Thought");

// Exporting an object containing the User and Thought models
module.exports = { User, Thought };
